import 'package:flutter/material.dart';

class ClipRRClass extends StatelessWidget {
  const ClipRRClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ClipRRect(

      ),
    );
  }
}

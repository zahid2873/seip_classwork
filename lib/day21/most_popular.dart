import 'package:flutter/material.dart';

class MostPopularInfo extends StatelessWidget {
  const MostPopularInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
